#!/bin/bash

PICTURE=$(mktemp /tmp/i3lock.XXXXXX.png)
BLUR="0x8"

if scrot -o "$PICTURE" && magick "$PICTURE" -blur "$BLUR" "$PICTURE"; then
    i3lock -i "$PICTURE"
    rm -f "$PICTURE"
else
    rm -f "$PICTURE"
    i3lock -c 282c34
fi
